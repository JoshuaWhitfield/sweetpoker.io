const log_in = '@log-in';
const sign_up = '@sign-Up';
const set_username = '@set_username';
const set_decal = '@set_decal';

const LogIn = () => {
    return {
        type: log_in,
        payload: null,
    }
}