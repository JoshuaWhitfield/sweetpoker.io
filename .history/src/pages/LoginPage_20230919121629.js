import Login from "../components/Login";

const LoginPage = () => {
    let height = '500px';
    let width = '450px';
    return (
        <Login width={width} height={height} />
    );
}

export default LoginPage