import Login from "../components/Login";

const LoginPage = () => {
    let height = '560px';
    let width = '550px';
    return (
        <Login width={width} height={height} />
    );
}

export default LoginPage