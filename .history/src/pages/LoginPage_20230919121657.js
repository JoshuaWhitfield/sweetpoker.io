import Login from "../components/Login";

const LoginPage = () => {
    let height = '460px';
    let width = '400px';
    return (
        <Login width={width} height={height} />
    );
}

export default LoginPage