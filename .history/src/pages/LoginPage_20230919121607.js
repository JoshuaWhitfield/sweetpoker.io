import Login from "../components/Login";

const LoginPage = () => {
    let height = '550px';
    let width = '400px';
    return (
        <Login width={width} height={height} />
    );
}

export default LoginPage